# --- AMBIENTE DE PRODUCAO ---
spring.profiles.active=prod

# --- CONEXAO COM O SUPABASE (PRODUCAO) ---
spring.datasource.url=jdbc:postgresql://db.hmhpxavrootipfmkoztw.supabase.co:5432/postgres
spring.datasource.username=postgres
# Considere usar variaveis de ambiente na hospedagem real: ${DB_PASSWORD}
spring.datasource.password=midvex-Sezfi0-nebxiq
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.maximum-pool-size=15
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=30000

# --- CONFIGURACAO DO HIBERNATE ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- CONFIGURACAO DE UPLOAD DE PDF ---
spring.servlet.multipart.max-file-size=15MB
spring.servlet.multipart.max-request-size=15MB

# --- AJUSTE DE DATA/HORA (PARA O RH DO HMLMB) ---
spring.jpa.properties.hibernate.jdbc.time_zone=America/Sao_Paulo
spring.jackson.serialization.write-dates-as-timestamps=false

# --- SEGURANCA E EXCECOES (PRODUCAO) ---
server.error.include-stacktrace=never
server.error.include-message=never

# --- CAMINHOS EXTERNOS ---
app.python.script.path=${APP_PYTHON_SCRIPT:scripts/extrair_dados.py}
app.python.executable=${APP_PYTHON_CMD:python3}

# --- CHAVE API DO GEMINI (GOOGLE AI STUDIO) ---
gemini.api.key=AIzaSyCliG-a3PZ_e8qI6uGAaD2SJRxHvHdWUaY
# CORRECAO: O modelo 'gemini-1.5-flash' nao esta disponivel na API v1beta gratuita. Usando o 'gemini-pro' que eh o padrao.
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-pro:generateContent?key=

# --- LOGGING ---
logging.level.root=INFO
logging.level.com.hmlmb.rh=INFO
logging.file.name=logs/rh-ferias-prod.log
